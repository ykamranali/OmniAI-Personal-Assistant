#if !defined(TORCH_STABLE_ONLY) && !defined(TORCH_TARGET_VERSION)
#pragma once
// @generated by torchgen/gen.py from DispatchKeyFunction.h

// NB: The implementing C++ file is RegisterDispatchKey.cpp

// The only #includes we need are for custom classes that have defaults in the C++ API
#include <c10/core/MemoryFormat.h>
#include <c10/core/Scalar.h>
#include <ATen/core/Reduction.h>

// Forward declarations of any types needed in the operator signatures.
// We can't directly include these classes because it will cause circular include dependencies.
// This file is included by TensorBody.h, which defines the Tensor class.
#include <ATen/core/ATen_fwd.h>

namespace at {

namespace compositeexplicitautograd {

TORCH_API at::Tensor _philox_uniform(const at::Tensor & self, const at::Tensor & key, double low=0, double high=1);
TORCH_API at::Tensor & _philox_uniform_out(at::Tensor & out, const at::Tensor & self, const at::Tensor & key, double low=0, double high=1);
TORCH_API at::Tensor & _philox_uniform_outf(const at::Tensor & self, const at::Tensor & key, double low, double high, at::Tensor & out);

} // namespace compositeexplicitautograd
} // namespace at

#else
#error "This file should not be included when either TORCH_STABLE_ONLY or TORCH_TARGET_VERSION is defined."
#endif  // !defined(TORCH_STABLE_ONLY) && !defined(TORCH_TARGET_VERSION)
